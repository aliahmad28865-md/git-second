name: cache job concurrency artifact

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    concurrency: 
      group: deploy
      cancel-in-progress: true

    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.0

      - name: Setup Node.js
        uses: actions/setup-node@v6.4.0
        with:
          node-version: 22.x
          cache: 'npm'

      - name: make build
        run: npm run build

      - name: Upload artifacts
        uses: astertom/nexus-upload-artifact@079c5f54b8f29dec49da7da7bb122ed248e9a626
        with:
          name: make-artifacts
          path: .next
          include-hidden-files: true
